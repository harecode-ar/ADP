import path from 'path'

export const ROOT_PATH = path.join(__dirname, '..', '..', '..')
export const FRONTEND_PATH = path.join(ROOT_PATH, 'packages', 'frontend')
export const BACKEND_PATH = path.join(ROOT_PATH, 'packages', 'backend')
export const SHARED_PATH = path.join(ROOT_PATH, 'packages', 'shared')
export const DEPLOY_PATH = path.join(ROOT_PATH, 'packages', 'deploy')

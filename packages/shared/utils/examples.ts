// ---------------------
// getAreaDescendantsIds
// ---------------------

// const areaId = 2 // Este id es del área que se quiere buscar.
// const cachedTree = await Cache.findOne({
//   where: {
//     key: ECacheKey.AREA_TREE,
//   },
// })
// if (!cachedTree) throw new Error('Árbol de áreas no encontrado')
// const tree: IArea[] = JSON.parse(cachedTree.value)
// const areaTree = getAreaFromTree(tree, areaId)
// if (!areaTree) throw new Error('Area no encontrada')
// const descendantsIds = getAreaDescendantsIds(areaTree)

// ---------------------
// getAreaDescendants
// ---------------------

// const areaId = 2 // Este id es del área que se quiere buscar.
// const cachedTree = await Cache.findOne({
//   where: {
//     key: ECacheKey.AREA_TREE,
//   },
// })
// if (!cachedTree) throw new Error('Árbol de áreas no encontrado')
// const tree: IArea[] = JSON.parse(cachedTree.value)
// const areaTree = getAreaFromTree(tree, areaId)
// if (!areaTree) throw new Error('Area no encontrada')
// const descendants = getAreaDescendants(areaTree)

// ---------------------
// getDirectAreaDescendantsIds
// ---------------------

// const areaId = 2 // Este id es del área que se quiere buscar.
// const cachedTree = await Cache.findOne({
//   where: {
//     key: ECacheKey.AREA_TREE,
//   },
// })
// if (!cachedTree) throw new Error('Árbol de áreas no encontrado')
// const tree: IArea[] = JSON.parse(cachedTree.value)
// const areaTree = getAreaFromTree(tree, areaId)
// if (!areaTree) throw new Error('Area no encontrada')
// const directDescendantIds = getDirectAreaDescendantsIds(areaTree)

// ---------------------
// getAreaAncestors
// ---------------------

// const areaId = 2 // Este id es del área que se quiere buscar.
// const cachedTree = await Cache.findOne({
//   where: {
//     key: ECacheKey.AREA_TREE,
//   },
// })
// if (!cachedTree) throw new Error('Árbol de áreas no encontrado')
// const tree: IArea[] = JSON.parse(cachedTree.value)
// const ancestors = getAreaAncestors(tree, areaId)
